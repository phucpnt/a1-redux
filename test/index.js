import 'mocha-loader!./connect.spec.js';

// if (process.env.NODE_ENV !== 'production') {
//   if (module.hot) {
//     module.hot.accept('../src', () => location.reload());
//   }
// }

document.title = 'A1 Redux > Unit test';
